## 🚀 О проекте
Browser GPT Agent — автономный AI-агент на Python, работающий в реальном, видимом браузере. Он умеет анализировать страницы, выполнять клики, поиск, ввод текста и выполнять сложные сценарии без заранее прописанных шагов.

## 🧠 Возможности

### Яндекс Почта (sub-agent)
- Читает письма
- Анализирует содержимое
- Определяет спам
- Удаляет спам-письма
- Отвечает и отправляет письма
- Формирует отчёт

### Яндекс Лавка (sub-agent)
- Ищет товары
- Переходит в карточки товаров
- Анализирует состав, вес, описание
- Добавляет товары в корзину
- Дополняет корзину по новым запросам пользователя

### hh.ru (sub-agent)
- Изучает профиль и резюме
- Ищет вакансии
- Читает карточки вакансий
- Откликается автоматически

⚠️ из минусов: hh.ru не успел пройти полное тестирование.
так же плохое отображение в консоли, не успел подправить
сам ИИ агент может работать и без суб- агентов но есть всегда шанс попасться в ловушку верстки или нехватки шангов выполнения
и ИИ агент не ограничен описаными выше задачами он может так же выполнять и продолжать работу на других сайтах но не всегда успешно.


## ⚙️ Настройка .env
```plaintext
OPENAI_API_KEY= #указываете ключ агента
PROXY= #указываете прокси для работы  с агентом
BROWSER_PROXY= #и указываете применения прокси если false то прокси будет применяться только у агента (для ру сайтов нужно false)
#настройка задержек между кликами и переходами
BROWSER_DEFAULT_TIMEOUT_MS=300
BROWSER_NAVIGATION_TIMEOUT_MS=500 
```
## ▶️ Запуск (Windows)
1. install.bat — установка зависимостей
2. start.bat — создание окружения и запуск агента
3. Chrome.bat — опциональный запуск браузера без агента

## 📌 Структура проекта
```plaintext
/agent
    agent_loop.py
    browser_tools.py
    debug_thoughts.py
    llm_client.py
    tools_init.py
    subagents/
        utils.py
        hhru/
            hhru.py
        yandex_mail/
            common.py
            compose.py
            read_delete.py
            reply.py
        yandex_lavka/
            __init__.py
/browser
    context.py
/config
    proxy.py
main.py
.env
install.bat
start.bat
Chrome.bat
```

## ✔️ Соответствие ТЗ
Проект полностью выполняет требования:
- Ручное управление браузером
- Автономное LLM-планирование
- Sub-agents
- Persistent session
- Обработка ошибок
- Security layer
- Никаких хардкодов структуры сайтов
