## 🚀 О проекте
Browser GPT Agent — автономный AI-агент на Python, работающий в реальном, видимом браузере. Он анализирует страницы, использует инструменты MCP и снимает срез Accessibility Tree, чтобы надежно выполнять клики, ввод текста и сложные сценарии без жёсткой привязки к DOM.

## 🧠 Возможности
- Управление браузером в несколько шагов с сохранением контекста между запросами.
- Универсальные инструменты (чтение, клики, ввод, скролл, скриншоты, snapshot_accessibility) в формате MCP.
- Автоматическое планирование шагов через LLM и детектор зацикливаний.
- Модульная система субагентов для аренды: поиск слотов, подготовка брони, безопасность на этапе оплаты.
- Persisted Playwright-сессия и лог мыслей агента (DEBUG_THOUGHTS).

## ⚙️ Настройка .env
```plaintext
LLM_PROVIDER=gpt # gpt или glm
OPENAI_API_KEY= # ключ OpenAI
OPENAI_MODEL= # модель OpenAI (опционально)
OPENAI_BASE_URL= # кастомный OpenAI-совместимый URL (по умолчанию https://api.openai.com/v1)
GLM_API_KEY= # ключ ZhipuAI (для glm)
GLM_MODEL=glm-4.6 # модель GLM (опционально)
GLM_BASE_URL=https://api.z.ai/v1 # базовый URL для GLM API
PROXY= # прокси для запросов к API (опционально)
BROWSER_PROXY= # включение/выключение прокси в браузере, false чтобы прокси был только у агента
# MCP transport (stdio по умолчанию)
MCP_SERVER_COMMAND=python # команда запуска MCP-сервера
MCP_SERVER_ARGS=-m agent.mcp_server # аргументы для запуска MCP-сервера
# таймауты по умолчанию
BROWSER_DEFAULT_TIMEOUT_MS=3000
BROWSER_NAVIGATION_TIMEOUT_MS=12000
BROWSER_DOM_STABLE_TIMEOUT_MS=8000
BROWSER_DOM_STABLE_INTERVAL_MS=500
BROWSER_DOM_STABLE_CHECKS=3
# дополнительные таймауты инструментов (опционально)
BROWSER_TOOL_ACTION_TIMEOUT_MS=5000
BROWSER_TOOL_DOM_STABLE_TIMEOUT_MS=5000
BROWSER_CLICK_TIMEOUT_MS=2000
BROWSER_CARD_CLICK_TIMEOUT_MS=4000
BROWSER_LOCATOR_ATTACH_TIMEOUT_MS=2000
BROWSER_LOCATOR_SCROLL_TIMEOUT_MS=2000
BROWSER_LOCATOR_CLICK_TIMEOUT_MS=2000
BROWSER_LOCATOR_FILL_TIMEOUT_MS=3000
BROWSER_TEXT_READ_TIMEOUT_MS=1000
BROWSER_TEXT_SNIPPET_TIMEOUT_MS=800
BROWSER_VISIBILITY_TIMEOUT_MS=1500
BROWSER_INPUT_VALUE_TIMEOUT_MS=800
BROWSER_BUTTON_ENABLED_TIMEOUT_MS=800
BROWSER_NAVIGATION_RETRY_MIN_TIMEOUT_MS=15000
AGENT_CONFIRMATION_TIMEOUT=60
GOOGLE_SEARCH_URL_TEMPLATE= # обязателен: URL шаблон поиска, например https://www.google.com/search?q={query}
```

Поисковый URL обязателен — агент не подставляет значения по умолчанию, поэтому задайте
`GOOGLE_SEARCH_URL_TEMPLATE` вручную перед запуском.

## 🪵 Пример консольного вывода (короткий статус)
По умолчанию агент печатает короткий UX‑лог. Полные мысли доступны при
`AGENT_DEBUG_THOUGHTS=1`.

```text
План → готов (4 шагов)
Автономный режим → старт
Шаг 1 → ответ получен
Инструмент read_view → ok
Шаг 2 → ответ получен
Инструмент click_by_text → ok
Ответ → получен

=== Итог по задаче: Забронировать слот на завтра ===
Статус: completed
Автономный отчёт: read_view: ok; click_by_text: ok; ...
```

## ▶️ Запуск (Windows)
1. install_agent.bat — установка зависимостей основного агента.
2. install_mcp.bat — установка зависимостей MCP-сервера в отдельное окружение.
3. install.bat — удобный запуск обоих установщиков подряд.
4. start.bat — создание окружения и запуск агента.
5. Chrome.bat — опциональный запуск браузера без агента.

## 🔌 MCP-сервер (tools layer)
Инструменты браузера регистрируются как MCP tools в процессе запуска агента. По умолчанию
клиент поднимает MCP-сервер через stdio-команду `python -m agent.mcp_server`. Если сервер
нужно запускать отдельно, используйте:

```bash
python -m agent.mcp_server
```

Для подключения к внешнему MCP-серверу переопределите переменные окружения:

```plaintext
MCP_SERVER_COMMAND=python
MCP_SERVER_ARGS=-m agent.mcp_server
```

Клиент MCP подключается через stdio-transport и автоматически подхватывает переменные
окружения процесса агента.

### ⚠️ Конфликты зависимостей и раздельные окружения
Если вы столкнулись с конфликтом версий (например, `fastapi` требует старый `starlette`,
а `mcp`/`sse-starlette` — новый), самый надёжный вариант — разнести зависимости по двум
виртуальным окружениям:

- **Основной агент**: остаётся в своём `.venv` с текущими зависимостями.
- **MCP-сервер**: запускается из отдельного `.venv_mcp` с более свежими библиотеками.

Пример для Windows (PowerShell/Git Bash), создаём отдельное окружение и ставим `mcp`:

```bash
py -3.12 -m venv .venv_mcp
source .venv_mcp/Scripts/activate
python -m pip install -U pip
python -m pip install mcp loguru
python -m agent.mcp_server
```

Затем в `.env` указываем путь к Python из этого окружения:

```plaintext
MCP_SERVER_COMMAND=C:\path\to\project\.venv_mcp\Scripts\python.exe
MCP_SERVER_ARGS=-m agent.mcp_server
```

Так MCP сможет использовать новые версии зависимостей, а основной агент останется на
совместимых пинах. Если же хочется оставить всё в одном окружении — придётся либо
обновлять/удалять конфликтующие пакеты, либо выносить dev-зависимости (например `tox`)
из runtime-окружения.

### 📦 Файлы зависимостей
- `requirements-agent.txt` — зависимости основного агента.
- `requirements-mcp.txt` — зависимости MCP-сервера (отдельное окружение).

## 📌 Структура проекта
```plaintext
/agent
    agent_loop.py
    browser_tools.py
    debug_thoughts.py
    llm_client.py
    tools_init.py
    subagents/
        base.py
        rental.py
        utils.py
/browser
    context.py
/config
    prompt_templates.py
    proxy.py
    sites.py
    timeouts.py
main.py
.env
install.bat
start.bat
Chrome.bat
```

## ✔️
- Нет жёстко прописанных URL или селекторов — все адреса настраиваются через .env.
- Субагенты реализуют модульную логику аренды (поиск → бронь → оплата), а не сайт-специфичные сценарии.
- Инструменты описаны в MCP-формате; доступен снимок Accessibility Tree для более точной навигации.
- Краткие системные промпты без сайт-специфичных инструкций.
- Устранены дублирующие конструкции и упрощён логгер мыслей.
